# Internal working files, not part of the published baseline
research.md
policy-review*.md
compass_artifact_*.md

# Distribution artefact built from the tracked files
files*.zip

# Python bytecode cache
__pycache__/
*.pyc

# Rendered production settings — carries a practice's real deployment values
# and must never be committed. See scripts/render-production-settings.py.
dist/
