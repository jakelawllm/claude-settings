{
  "$schema": "https://json.schemastore.org/claude-code-settings.json",
  "_comment": "PER-MATTER PROJECT FILE — convenience layer only. Mandatory transmission controls (WebFetch, WebSearch) belong in managed-settings.json. This file supplements but does not replace managed policy.",
  "permissions": {
    "deny": [
      "WebFetch",
      "WebSearch"
    ]
  }
}
